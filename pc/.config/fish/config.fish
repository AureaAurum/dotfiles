# PC-specific fish config: source common settings installed in $HOME
if test -f $HOME/.config/fish/common_config.fish
    source $HOME/.config/fish/common_config.fish
end

# PC-specific overrides can be added below
